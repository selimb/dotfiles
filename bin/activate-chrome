#!/usr/bin/env bash

chrome="Google Chrome"
activate="wmctrl -a $chrome"

# If arguments are specified, launch google search on it.
if [[ -n "$1" ]]; then
    xdg-open "$1"
elif [[ -z $(wmctrl -l | grep "$chrome") ]]; then  #Check if Chrome is not running
    google-chrome
fi
$activate
exit 0